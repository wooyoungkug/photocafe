name: PostgreSQL Backup to B2

on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch:

jobs:
  backup:
    name: Backup PostgreSQL to Backblaze B2
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set timestamp and backup type
        id: meta
        run: |
          export TZ="Asia/Seoul"
          DATE=$(date '+%Y-%m-%d')
          TIME=$(date '+%H%M%S')
          DOW=$(date '+%u')
          DOM=$(date '+%d')

          PREFIX="daily"
          if [ "$DOM" = "01" ]; then
            PREFIX="monthly"
          elif [ "$DOW" = "7" ]; then
            PREFIX="weekly"
          fi

          FILENAME="photocafe-${DATE}-${TIME}.sql.gz"
          S3_KEY="${PREFIX}/${FILENAME}"

          echo "filename=${FILENAME}" >> $GITHUB_OUTPUT
          echo "s3_key=${S3_KEY}" >> $GITHUB_OUTPUT
          echo "prefix=${PREFIX}" >> $GITHUB_OUTPUT
          echo "📦 Backup type: ${PREFIX}"
          echo "📁 S3 key: ${S3_KEY}"

      - name: Dump PostgreSQL database (Docker)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "🗄️  Starting pg_dump via Docker (postgres:18)..."
          echo "🔗 Connecting to: ${DATABASE_URL//:*@/::****@}"

          docker run --rm \
            postgres:18 \
            pg_dump "$DATABASE_URL" \
            --no-owner \
            --no-privileges \
            --format=plain \
            --verbose \
            2> dump.log \
            | gzip -9 > "${{ steps.meta.outputs.filename }}"

          echo "=== pg_dump 에러 로그 ==="
          cat dump.log || echo "(로그 없음)"
          echo "========================="

          SIZE=$(du -h "${{ steps.meta.outputs.filename }}" | cut -f1)
          echo "✅ Dump complete. Size: ${SIZE}"
          ls -lh "${{ steps.meta.outputs.filename }}"

      - name: Verify dump integrity
        run: |
          gzip -t "${{ steps.meta.outputs.filename }}"
          echo "✅ Gzip integrity OK"

          if zcat "${{ steps.meta.outputs.filename
